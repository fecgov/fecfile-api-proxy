# The entire if condition is needed to block an IP

if ($http_x_forwarded_for ~* "(^|,\s*)185\.177\.72\.13(\s*,|$)") {
    return 403;
}
if ($http_x_forwarded_for ~* "(^|,\s*)64\.252\.89\.202(\s*,|$)") {
   return 403;
}